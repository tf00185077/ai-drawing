---
description: AI 依使用者描述組 workflow 並生圖（由 Cursor AI 直接產生 workflow，不需 OpenAI API）
alwaysApply: false
globs: 
---

# MCP 依描述生圖指引

## 設計：由 AI 直接產 workflow

Workflow 由 **你（Cursor AI）** 依使用者描述自行組出，無需後端呼叫 OpenAI。

## 流程

| 情境 | 作法 |
|------|------|
| 簡單（角色+風格，如初音動漫） | `generate_image_from_description("初音 動漫 1024")` |
| 需 ControlNet、img2img 等預存模板沒有的 | 你自行組 workflow → `generate_image_custom_workflow(workflow=..., prompt=..., character=...)` |

## 自行組 workflow 步驟

1. `list_workflow_templates` → 看有哪些模板
2. `get_workflow_template("default")` 或 `get_workflow_template("default_lora")` → 取得 base JSON
3. 依描述修改節點（加入 ControlNet、img2img 等），產出完整 workflow
4. `generate_image_custom_workflow(workflow=<JSON 字串>, prompt=..., character=..., width=..., height=...)` 提交

## ComfyUI 常見節點

- **CheckpointLoaderSimple**: ckpt_name
- **LoraLoader**: lora_name, strength_model, strength_clip, model, clip（連線）
- **CLIPTextEncode**: text, clip（連線）
- **EmptyLatentImage**: width, height, batch_size
- **KSampler**: model, positive, negative, latent_image, seed, steps, cfg, sampler_name, scheduler, denoise
- **VAEDecode**: samples, vae
- **SaveImage**: images, filename_prefix
- **LoadImage**: image（檔名，img2img 用）
- **VAEEncode**: pixels, vae（img2img  latent）
- **ControlNetLoader**: control_net_name
- **ApplyControlNet**: positive, control_net, image

連線格式：`["節點ID", 輸出索引]`，如 `["4", 1]`。
