---
description: 遠端觸發生圖 - Slack Socket Mode 實作規範
globs: backend/app/main.py, backend/app/services/slack_handler.py, backend/app/services/slack_commands.py, backend/app/core/queue.py
alwaysApply: false
---

# Slack 遠端觸發生圖規範

> 凡實作或修改 Slack 觸發生圖相關邏輯時，必須遵循此規範。供所有 Agent 共用參考。

---

## 1. 架構原則

### 1.1 連線方式

- **Slack Socket Mode**：後端主動建立 WebSocket 連到 Slack，無需公網 IP、ngrok 或開放 port
- **單一客戶端**：同一後端進程內只維持一條 Socket Mode 連線
- **與 FastAPI 共生**：Socket Mode 客戶端在後端啟動時一併啟動，與 HTTP API 同進程

### 1.2 職責分離

| 元件 | 職責 | 禁止 |
|------|------|------|
| **slack_handler** | 解析 Slack 事件、驗證、組成生圖參數、回覆 Slack | 不直接呼叫 ComfyUI |
| **queue** | 接收生圖請求、排程、呼叫 ComfyUI | — |
| **main.py / lifecycle** | 啟動 Socket Mode 客戶端、註冊 event handler | 不處理業務邏輯 |

**Handler 必須**：透過內部 HTTP 呼叫 Backend API（`/api/generate/`、`/api/generate/custom`、`/api/lora-train/start`、`/api/gallery/` 等），或 legacy 流程呼叫 `queue.submit()`；不得直接操作 ComfyUI。

---

## 2. 作法（實作流程）

### 2.1 啟動流程

1. 後端啟動（`uvicorn` / `lifespan`）
2. 讀取 `SLACK_APP_TOKEN`、`SLACK_BOT_TOKEN`（若未設定則跳過，不啟動 Slack）
3. 建立 `slack_bolt.App`（Socket Mode）
4. 註冊 `message` 事件 handler，指向 `slack_handler.handle_message`
5. 在背景執行 `app.start()`（非 blocking，避免影響 FastAPI）

### 2.2 事件處理流程

```
Slack message 事件 → slack_handler.handle_message()
    → 過濾 bot 自身訊息（避免迴圈）
    → 解析指令（如 "!generate 初音 5張"）
    → 映射 prompt（可結合 character_style）
    → 組成 GenerateParams（與 /api/generate/ 同 schema）
    → queue.submit(params)
    → 以 chat.postMessage 回覆 Slack（佇列狀態或錯誤）
```

### 2.3 指令格式

- **規格唯一來源**：`backend/app/services/slack_commands.py` 的 `COMMAND_SPECS`
- 觸發關鍵字、必填/選填、範例、說明皆以 COMMAND_SPECS 為準，不得在此重複定義
- Handler 透過 `parse_command()`、`validate_params()`、`build_help_message()` 取得指令規格
- 未符合格式時回覆：「參數格式錯誤，請用 !給我可用指令 查看」

### 2.4 指令與 API 對照（引用 COMMAND_SPECS）

| cmd_key | 對應 API | 說明 |
|---------|----------|------|
| help | 無 | `build_help_message()` |
| generate | POST /api/generate/ | 依 prompt 生圖 |
| generate_pose | GET workflow-templates + POST /api/generate/custom | 生圖 + 姿態參考圖 |
| train_lora | POST /api/lora-train/start | 手動觸發 LoRA 訓練 |
| query_gallery | GET /api/gallery/ | 圖庫列表 |
| rerun | POST /api/gallery/{id}/rerun | 用某張圖參數再產 |

**指令規格（triggers、required、optional、example、desc）**：見 `backend/app/services/slack_commands.py` 的 `COMMAND_SPECS`。

---

## 3. 通用接口

### 3.1 Handler 輸入

`slack_handler.handle_message(client, event, logger)` 或同等簽名：

- `client`: Slack Bolt `Say` / `Ack` 等，用於回覆
- `event`: Slack `message` 事件 payload（含 `text`、`channel`、`user` 等）
- `logger`: 用於紀錄 log

### 3.2 與 Backend API 的對接

Handler 使用 `internal_api_base_url` 透過內部 HTTP 呼叫 Backend API，不得直接操作 ComfyUI。對應關係見 §2.4。

### 3.3 回覆 Slack 的資料格式

依 COMMAND_SPECS 對應之 API 回傳 status 決定：

| 指令 | 情境 | 回覆內容 |
|------|------|----------|
| help | 成功 | `build_help_message()` 文案 |
| generate, generate_pose | 201 | `已加入生圖佇列，job_id: xxx` |
| generate, generate_pose | 503 | `生圖佇列已滿` |
| generate, generate_pose | 400 | `參數錯誤：{detail}` |
| train_lora | 202 | `已加入訓練佇列，job_id: xxx` |
| train_lora | 400/409 | `操作失敗：{detail}` |
| query_gallery | 200 | `共 {total} 筆，最近：id=1 prompt=...` |
| rerun | 202 | `已加入生圖佇列，job_id: xxx` |
| rerun | 404 | `找不到該圖片` |
| rerun | 503 | `生圖佇列已滿` |
| 共用 | 解析失敗 | `參數格式錯誤，請用 !給我可用指令 查看` |
| 共用 | 網路/服務不可用 | `生圖服務暫不可用` / `訓練服務暫不可用` / `圖庫服務暫不可用` |

**禁止**：對 Slack 回傳 500 或未經處理的 exception 訊息；一律回覆友善提示並紀錄 log。

### 3.4 Backend 執行中錯誤的失敗通知

**使用者透過指令觸發 Backend 後，若 Backend 執行中發生錯誤（例如 ComfyUI 連線失敗、佇列 worker 無法完成任務），必須透過 Slack 主動發送失敗通知。**

- **時機**：指令已回覆「已加入佇列」後，佇列 worker 執行任務時發生錯誤（如 `httpx.ConnectError`、ComfyUI 不可用）。
- **實作**：任務提交時傳入 `slack_channel_id`、`slack_thread_ts`（來自觸發指令的 event）；queue 於 `_process_pending` 捕獲錯誤、標記任務失敗時，呼叫 `slack_notifier.notify_job_failed()` 發送訊息。
- **訊息範例**：`生圖任務 {job_id} 執行失敗：ComfyUI 連線失敗，請確認服務已啟動`
- **檔案**：`backend/app/services/slack_notifier.py`、`backend/app/core/queue.py`

---

## 4. 安全

- **敏感資訊**：`SLACK_APP_TOKEN`、`SLACK_BOT_TOKEN` 僅從 `config` / 環境變數讀取，不得硬編碼
- **權限**：建議 Bot 僅加入受信任的私有頻道，避免陌生人觸發生圖
- **簽章**：Socket Mode 由 Slack 推送，連線本身已受 Token 保護；不需額外驗證簽章（與 HTTP Webhook 不同）

---

## 5. 錯誤處理

| 錯誤類型 | 處理方式 |
|----------|----------|
| QueueFullError | 回覆 User「生圖佇列已滿，請稍後再試」 |
| 解析失敗 | 回覆「無法理解，請輸入生圖描述」；紀錄 log |
| ComfyUI 不可用 | 回覆「生圖服務暫不可用」 |
| 任務執行中錯誤（ComfyUI 連線失敗等） | 任務失敗時透過 `slack_notifier` 主動發送 Slack 失敗通知至觸發頻道 |
| Slack API 錯誤 | 紀錄 log，不拋出 500 給使用者 |

**原則**：對外（Slack User）一律回覆簡短友善訊息；詳細錯誤僅寫入 log。若任務在執行中失敗，須主動通知 Slack。

---

## 6. 檔案對應

| 職責 | 檔案路徑 |
|------|----------|
| **指令規格唯一來源** | `backend/app/services/slack_commands.py`（`COMMAND_SPECS`） |
| Socket Mode 啟動 / lifecycle | `backend/app/main.py` 或 `backend/app/core/slack_lifecycle.py` |
| 訊息解析與觸發 | `backend/app/services/slack_handler.py` |
| 生圖佇列 | `backend/app/core/queue.py` |
| 任務失敗通知 | `backend/app/services/slack_notifier.py` |
| 環境變數 | `.env`：`SLACK_APP_TOKEN`、`SLACK_BOT_TOKEN` |

---

## 7. Agent 實作檢查清單

實作或修改時請確認：

- [ ] Handler 透過內部 HTTP 呼叫 Backend API（見 §2.4），不直接呼叫 ComfyUI
- [ ] Token 從 config 讀取，無硬編碼
- [ ] 過濾 bot 自身訊息，避免事件迴圈
- [ ] 佇列滿、解析失敗時回覆友善訊息，不拋 500
- [ ] 任務執行中錯誤（如 ComfyUI 連線失敗）時，透過 slack_notifier 發送 Slack 失敗通知
- [ ] 所有異常皆紀錄 log
