---
description: AI 自動化出圖系統專案規格與架構指引
alwaysApply: true
---

# AI 自動化出圖系統 - Cursor Rule

**Agent 必讀順序**：
1. `AGENTS.md` — 架構、Tech Stack、編碼慣例、Skill 對應
2. `docs/GOAL.md` — 系統目標與現在聚焦的範圍
3. `docs/PROGRESS.md` — 現在做到哪、下一步是什麼（唯一進度來源）
4. `docs/task-specs/phase{N}.md` — 要做的 task 的完整 spec

## 核心原則

- **Agent-first**：MCP Tools 的完整性優先於前端 UI
- **單一來源**：進度只更新 `docs/PROGRESS.md`，不改 README 或 AGENTS.md
- **驗證明確**：每個 task 都有 verify 指令，見 `docs/task-specs/` 與 `docs/agent-framework.md`
- **安全第一**：KEY / Token 一律走環境變數，禁止硬編碼

## Skill 優先參考

實作前先查閱對應 `.cursor/skills/` 下的 SKILL.md：

| 領域 | Skill |
|------|-------|
| ComfyUI（生圖、workflow、佇列） | `comfyui-api-client` |
| Workflow JSON 組裝 | `comfyui-workflow` |
| LoRA 模組 | `lora-train-docs` |
| Kohya sd-scripts | `kohya-sd-scripts` |
| WD Tagger | `wd-tagger` |
| Python 後端擴展性審核 | `python-extensibility-review` |
| MCP Tools 呼叫 | `mcp-tools-usage` |

## Tech Stack（固定，勿替換）

- 後端: Python 3.11 + FastAPI
- 前端: React 18 + Vite + Tailwind（.tsx）
- 資料庫: SQLite / PostgreSQL（SQLAlchemy ORM）
- 圖片引擎: ComfyUI API（WebSocket + REST）
- MCP: Python MCP SDK（`mcp-server/`）
