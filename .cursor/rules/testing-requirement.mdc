---
description: 可複用功能完成後需補測試，至少兩條用例
alwaysApply: true
---

# 測試補寫規範

功能實作完成後，**若該功能會被複用**，必須補上單元測試，且至少包含 **兩條測試用例**。

## 適用範圍

- 後端：`backend/**/*.py`（工具函數、服務、核心邏輯）
- 前端：`frontend/src/**/*.{js,jsx,ts,tsx}`（可複用 hooks、utils）

## 範例

| 情境 | 是否需補測試 |
|------|--------------|
| API 端點、核心服務函數 | ✅ 需補，至少 2 條 |
| 僅用於單一頁面的 UI 元件 | ❌ 可略過 |
| 工具函數（如 workflow 替換、參數解析） | ✅ 需補，至少 2 條 |
| 單次性腳本 | ❌ 可略過 |
