# 進度追蹤

> **唯一來源**。完成的任務要同步修改這個文件（`docs/PROGRESS.md`），且不需同步改 README.md 或 AGENTS.md。
> 完整 task spec 見 `docs/task-specs/`。

---

## 目前聚焦

OpenClaw × ai-drawing 本地繪圖 / MCP 整合已建立交接計畫：`docs/openclaw-ai-drawing-mcp-handoff.md`。

目前執行位置：Phase 1~5 文件化流程已完成。最小 MCP 閉環已通過單元測試與本機實測，且 OpenClaw MCP 繪圖 SOP 已整理完成。

---

## 已完成

### 修復
- [x] MCP tool catalog / LoRA exposure audit（2026-07-06，`fix-mcp-tool-catalog-exposure`）：新增 `mcp_server/tool_catalog.py` 作為 43 個 intended MCP tools 的機器可測 catalog，測試會比對 FastMCP server-side registration、tool module function、response category、README 與 `docs/mcp-setup.md` 的 active catalog / intentional omissions。診斷 `generate_video_custom_workflow`：repo/server-side 註冊正確，`mcp.list_tools()` 可見且 schema 目前含 `checkpoint`/`lora`/`lora_strength`/`loras`；Hermes session 看不到的原因判定為 client/session tool catalog cache 或未 reload，文件已補 reload requirement。修正 LoRA exposure：`list_available_resources` 將 `loras`/`video_loras` 穩定正規化為 list；backend resource inventory smoke 回 `loras_count=21`、`workflows_count=32`；style preset validation 逐一檢查 ordered `loras`；video custom workflow MCP tool 會轉送 `checkpoint`、單一 `lora`、`lora_strength` 與 ordered multi-`loras`；backend API 轉送 `loras` 時排除 `strength_clip=None`。response contract：LoRA training tools維持 dict；既有 JSON-string transitional tools 保持相容但 touched machine-facing error path 改為 `ok=false/tool/error.code/message/details`。
- [x] LoRA training MCP workflow（2026-07-06）：新增 backend-managed LoRA 訓練流程與 MCP structured JSON tools。支援 `lora_dataset_list/inspect/prepare/validate`、`lora_train_start/job_status/logs/cancel/smoke_test`；dataset prepare 支援 dry-run/apply/backup/restore 與穩定 trigger token 正規化；訓練 job 以 DB 持久化 status/stage/progress/epoch/log/output/registered LoRA/error；MCP tool 回 `ok/tool/error` 結構，agent 不需解析文字。實測：重啟 backend 後用 MCP tool 實際連 `127.0.0.1:8001` 完成 list→inspect→prepare dry-run→apply→validate→status→job/logs/smoke error path，並驗證缺 `sd-scripts` 時 `lora_train_start` 立即 structured 400，不產生新 failed job。Batch gate 複查：本機 `SD_SCRIPTS_PATH=./sd-scripts` 解析後不存在，且搜尋 repo 未找到 `train_network.py`、`sdxl_train_network.py` 或 `finetune/tag_images_by_wd14_tagger.py`；以臨時 2-image dataset 跑 backend in-process smoke 完成 list→inspect→prepare dry-run→apply→validate，start 回 structured 400 `sd_scripts_path_missing` 且 `persistent_job_count=0`、aggregate status `idle`。OpenSpec 5.4 已拆分：本 change 完成可本機驗證的 workflow/preflight；真實 Kohya start→poll/logs→register→smoke runtime happy path 移至 `install-verify-kohya-sd-scripts-runtime` follow-up。
- [x] LoRA trainer runtime preflight（2026-07-06）：`lora_trainer.enqueue()` 在建立 job / 寫 DB / append queue 前驗證 `sd_scripts_path` 存在且為目錄，並依 SDXL 設定確認 `train_network.py` 或 `sdxl_train_network.py` 存在；缺失時回 `TrainerServiceError`，`/api/lora-train/start` 轉為 structured 400，MCP `lora_train_start` 可立即得到 `ok=false` 而不會產生瞬間 failed job。測試：`pytest backend/tests/test_lora_trainer.py backend/tests/test_lora_train_workflow_api.py -q` → `25 passed`。
- [x] `list_resources` tool 回傳 `{}`（2026-06-17）：tool 名稱與 MCP 協定內建的 `resources` primitive（`resources/list`）撞名，agent 端被路由到沒掛任何 resource 的 `resources/list` → 空 `{}`，而非真正的 tool。backend 與 tool 邏輯本身正常。改名為 `list_available_resources`（與 backend `available-resources` 端點同名、避開 primitive），同步更新 `generate.py`、tests、`mcp-server/README.md`、`docs/mcp-setup.md`、`docs/openclaw-mcp-drawing-sop.md`。歷史規劃/SOP 記錄文件保留原名不動。`uv run pytest tests/ -q` → `43 passed`。
- [x] MCP tool 呼叫時 `McpSettings` 驗證錯誤（`Extra inputs are not permitted`，2026-06-17）：root cause 是 `config.py` 用了 pydantic 核心的 `ConfigDict` + 相對路徑 `env_file=".env"`，Hermes 從 `~/.hermes` 啟動時把 `~/.hermes/.env` 整包當成 extra 欄位吃進來。改為 `SettingsConfigDict(env_prefix="MCP_", extra="ignore")` 並移除 `env_file`，只讀 `MCP_*` 真實環境變數。已在汙染 CWD `.env` + process env 下驗證初始化正常。

### OpenClaw × ai-drawing 本地繪圖 / MCP 整合
- [x] Phase 1 backend 低負載繪圖驗證（2026-06-11）：backend `8001` → ComfyUI `8188` → gallery 閉環成功，job `27920202-569f-4880-abc2-7a9f477d0094`，輸出 PNG 512×512。
- [x] Phase 2 OpenClaw backend 繪圖 SOP（2026-06-11）：建立 `docs/openclaw-backend-drawing-sop.md`，包含可用 curl/HTTP 範例、成功/失敗判斷、資源限制與 busy 處理規則。
- [x] Phase 3 Step 1 MCP config / URL（2026-06-11）：`config.py` 預設 `8001`，新增 `test_config.py`，`uv run python -m pytest tests/ -q` → 通過。
- [x] Phase 3 Step 2 `list_resources`（2026-06-11）：agent-friendly JSON，`5 passed`。
- [x] Phase 3 Step 3 `generate_image`（2026-06-11）：回傳含 `job_id/status/submitted/next`，`4 passed`。
- [x] Phase 3 Step 4 `get_generation_status`（2026-06-12）：queued/running/completed/error 四種狀態，`10 passed`，完整回歸 `42 passed`。
- [x] Phase 3 Step 5 `get_gallery_image`（2026-06-12）：回傳 `image_url`、`local_path`、metadata 與 JSON 錯誤結構，`uv run pytest tests/test_tools.py -k 'gallery_image or gallery_detail' -q` → `4 passed`。
- [x] Phase 3 Step 6 `free_comfyui_memory`（2026-06-12）：新增 ComfyUI `/free` wrapper，`uv run pytest tests/ -k 'free_comfyui_memory or comfyui' -q` → `3 passed`。
- [x] Phase 3 Step 7 registration / docs（2026-06-12）：MCP server 載入新 tools，`docs/mcp-setup.md` 與 `mcp-server/README.md` 已同步最小閉環 tool 清單，`uv run pytest tests/test_server.py -q` → `3 passed`。
- [x] Phase 3 Step 8 完整單元測試（2026-06-12）：`uv run pytest tests/ -q` → `50 passed`。
- [x] Phase 3 Step 9 本機 backend / ComfyUI MCP smoke test（2026-06-12）：以 MCP tools 完成 `list_resources → generate_image → get_generation_status → get_gallery_image → free_comfyui_memory` 閉環；job `c99167aa-4370-47e1-ae09-2b97d5f18978`、image `2`、輸出 `outputs/gallery/2026-06-12/ComfyUI_00008__c99167aa_0.png`、PNG 512×512，測試後 queue 為空。
- [x] Phase 4 Hermes / agent MCP 實際驗證（2026-06-12）：已由 Hermes 實際透過 MCP tools 完成一次生圖與查詢，不再依賴 curl/HTTP 手動操作。
- [x] Phase 5 OpenClaw MCP 繪圖 SOP（2026-06-15）：建立 `docs/openclaw-mcp-drawing-sop.md`，根據本機 `openclaw mcp show` 設定與真 stdio MCP 驗證整理 OpenClaw agent 使用規則、`openclaw mcp set` 範例與最小閉環操作順序；當日再次實測 job `3a18d370-3726-4fcf-b91b-b838fb6e4b87`、image `6`、輸出 `outputs/gallery/2026-06-15/ComfyUI_00012__3a18d370_0.png`。
- [x] 記錄 Anima 元件以支援重生（2026-06-17）：`GeneratedImage` 新增 `template` / `diffusion_model` / `text_encoder` / `vae` 四欄。`workflow.py` 加 `extract_model_files_from_workflow`，並讓 `apply_params` 支援注入 `UNETLoader.unet_name`（diffusion_model，退回 checkpoint）/ `CLIPLoader.clip_name`（text_encoder）/ `VAELoader.vae_name`（vae）。`queue.py` 於送出後反解實際 workflow 的模型檔並寫回 job 參數 → `recording.save()` 一併記錄。`gallery_rerun` 帶回 template + 三個元件，確保 Anima 等非傳統 checkpoint 可正確重生；gallery 列表/詳情/CSV 匯出與 MCP `get_gallery_image` metadata 也補上這些欄位。`scripts/init_db.py` 加 idempotent `ensure_columns()`（ALTER TABLE ADD COLUMN），已對既有 `auto_draw.db` 補欄位（保留原 5 筆）。backend `113 passed`、mcp-server `43 passed`（4 既有失敗無關），並以單元模擬驗證「生圖→記錄→重生」模型檔一致。
- [x] diffusion-model 元件目錄與資源列舉（2026-06-17）：新增 `comfyui_diffusion_models_dir` / `comfyui_text_encoders_dir` / `comfyui_vae_dir` 三個設定（`config.py` 預設 + `.env` / `.env.example`，本機指向 `/Volumes/AI-Drawing-16T/ai-drawing/models/{diffusion_models,text_encoders,vae}`）。`resources.py` 加 `list_diffusion_models` / `list_text_encoders` / `list_vaes`，`/api/generate/available-resources` 與 MCP `list_resources` 一併回傳 `diffusion_models` / `text_encoders` / `vaes`。實測抓到 `anima_preview3Base.safetensors`、`qwen_3_06b_base.safetensors`、`qwen_image_vae.safetensors`。
- [x] 多 workflow 模板切換（Anima 支援，2026-06-17）：`generate_image` 新增 `template` 參數，可在不另開 tool 的情況下切換模板（如 `anima`）。後端 `apply_params` 支援 diffusion-model 家族節點（`UNETLoader.unet_name`、`EmptySD3LatentImage` 寬高/batch）；`queue.py` 改為「自訂 workflow > 指定 template > 依 lora 推斷預設」優先序，且僅含 `CheckpointLoaderSimple` 的傳統 workflow 才套用預設 checkpoint，避免把傳統 checkpoint 名稱注入 Anima 的 UNETLoader。動到 `mcp-server/.../generate.py`、`backend/app/schemas/generate.py`、`backend/app/api/generate.py`、`backend/app/core/queue.py`、`backend/app/core/workflow.py`。backend `113 passed`、mcp-server `50 passed`（4 個既有失敗與本次無關）。
- [x] 移除重複的純文字 tool（2026-06-17）：刪掉 `get_job_status`、`get_available_resources`、`gallery_detail` 三個早期純文字版，只保留對應的 agent-friendly 結構化版（`get_generation_status`、`list_resources`、`get_gallery_image`）。一併清掉相關測試與 README 條目；server 註冊 tool 由 23 → **20**，skill 未參照這三者故不受影響。mcp-server `43 passed`。
- [x] MCP README tool 清單同步（2026-06-17）：`mcp-server/README.md` 更新為完整 23 個 tool（分組並標示與純文字版重疊的 JSON 版），補上先前漏列的 `get_generation_status`、`get_job_status`、`cancel_job`、`list_resources`、`get_available_resources`、`caption_image`、`get_gallery_image`、`free_comfyui_memory`。
- [x] `add-style-preset-catalog`（openspec change，2026-06-19）：新增風格預設目錄（創作者／風格食譜）。後端 `backend/app/core/style_presets.py` 以 JSON（`backend/style_presets/catalog.json`，sample-safe 空清單）為執行期來源，提供 `list_presets`/`get_preset`/`validate_presets`/`compose`，prompt 依固定順序組裝（base → profile prefix → content_prompt → profile suffix），負面 prompt 兩層合併，參數優先序 default_params < profile params < overrides；`validate_presets` 會同時檢查模型/template 資源、`note_path` 是否存在、Markdown frontmatter `preset_id` 是否與 catalog `id` 一致。新增 `GET/POST /api/style-presets`（list / `{id}` / validate / `{id}/compose`）與 schemas；`compose` 採 compose-first-generate-second，回傳可直接餵 `generate_image` 的 `generation` payload。一般生圖 `GenerateRequest` + `trigger_generate` 補上 `diffusion_model`/`text_encoder`/`vae`，讓 Anima 等 diffusion-family preset 不必改走 `/custom`。MCP 新增 `list_style_presets`/`get_style_preset`/`validate_style_presets`/`compose_style_preset` 四個 agent-friendly tool（`ok`/`tool`/`next`/結構化錯誤），`generate_image` 同步補三個 diffusion 元件參數。文件：`mcp-server/README.md`（20→24 tool + 新章節）、`docs/api-contract.md`（5c 風格預設模組 + 生圖新欄位）、`docs/openclaw-mcp-drawing-sop.md`（preset 模式／手動模式分流，僅在無對應 preset 時才問是否新增）。測試：backend 新增 `test_style_presets.py`（18）+ `test_style_presets_api.py`（10）；mcp-server 新增 `test_style_presets.py`（10）+ e2e dry path `test_style_preset_e2e.py`（1，compose→generate_image 轉送）。backend `149 passed`、mcp-server `56 passed`（4 個 backend 既有失敗與本次無關：config-path cwd、兩個 lora_trainer mock、缺檔 `honoka_pose_controlnet` 模板）。
- [x] `harden-queue-completion`（openspec change，2026-06-20）：修掉 `_check_running_complete` 會讓 job 靜默消失的缺口（取代 `docs/backend-generate-queue-head-blocking-2026-06-16.md` 的待決事項）。新增 `comfyui.structure_execution_error`（history 的 `execution_error` → `[{node_id,class_type,reason}]`，同 #4 形狀）。重寫完成判定：(1)「仍在處理」改判 ComfyUI `queue_running` **或** `queue_pending`；(2) 離開佇列後依 `history.status_str` 決定終局，而非只看有無圖；(3) history 延遲 → 保留 running、有上限重試（`MAX_COMPLETION_POLLS=15`，`_Job.completion_polls`），超時才標 failed 並釋放槽；(4) `status_str=="error"` → failed（結構化 execution_error）；(5) success 有圖 → 存檔 completed，**save 例外也轉 failed 不靜默丟**；(6) success 無圖 → failed。淨保證：**每個終局 job 必落在 completed(DB) 或 failed(帶原因)其一**，這也讓 #4 的 failed 涵蓋執行期錯誤、並使 #5 的 DB 閘門可信。測試：backend 新增 `test_queue_completion.py`（6：pending 留 running、lag→重試後成功、逾時 failed、execution error 結構化、無圖 failed、save 例外 failed）；全量 backend `200 passed`（4 既有失敗無關）。真機:silent-drop 路徑未觸發、job 在 ComfyUI 佇列期間正確留 running（live 全程完成因 ComfyUI 當下積壓/error 未觀察到，邏輯以單元測試覆蓋）。
- [x] `add-multi-lora-support`（openspec change，2026-06-21）：補上「多 lora」通道,修掉整條鏈只支援單一 lora 的漏洞。新增 `loras: [{name, strength_model, strength_clip?}]` 有序清單,與單一 `lora`/`lora_strength` 並存(有 `loras` 時優先)。`apply_params` 收集 workflow 內 `LoraLoader`/`LoraLoaderModelOnly` 節點(依 JSON 順序),`loras[i]→第 i 個 loader`(clip 省略時沿用 model;model-only 跳過 clip;多出忽略;未提供 loras 時維持單一 lora 灌全部的舊行為)。貫穿:`GenerateParams`(queue)、`LoraSpec`+`GenerateRequest/GenerateCustomRequest.loras`、`/api/generate` 兩端點、MCP `generate_image`/`generate_image_custom_workflow`;preset 端 `StylePreset.loras`+`_parse_preset`+`compose_preset`(emit `generation.loras`,多 lora 時不帶單一 lora)+`build_summary`,schema `StylePresetDetail/Summary/CreatePresetRequest.loras`,MCP `create_style_preset(loras=)`。spec 改 `custom-workflow-generation`(位置式多 lora 注入)與 `style-preset-catalog`(presets 帶 loras 清單)。測試:backend `test_multi_lora.py`(7:位置對應/單一相容/loras 優先/多出忽略/model-only 跳 clip/compose emit/單一不變)、mcp-server +2;全量 backend `237 passed`、mcp-server `75 passed`(既有失敗從 4 降為 3,bbox 測試轉綠)。向後相容:既有單一 lora preset/template 行為不變。
- [x] `add-style-preset-authoring`（openspec change，2026-06-20）：新增「依描述建立 preset」的寫入能力。`DirStylePresetProvider.create_preset(fields, create_note, overwrite)`：驗 id slug／name 必填、detail 已存在且未 overwrite 則 `PresetExistsError`、自動推 `note_path=style_presets/human/<id>.md`、寫機器食譜 `presets/<id>.json` + 人類 note stub（frontmatter `preset_id` 對齊，通過 note 驗證）+ reindex；新增 `validate_preset(id, inv)`。新增 `PresetExistsError`/`is_valid_preset_id`/`_note_stub`。API `POST /api/style-presets/`（201；重複 409；id/name 不合法 422；回傳 created + 非阻斷 validation）。MCP `create_style_preset(id, name, …, overwrite)`（409→already_exists、422→invalid_request、缺資源於 next 提示但不擋；tool 數 31→32）。測試：backend `test_style_preset_authoring.py`（9）、mcp-server +2；全量 backend `227 passed`、mcp-server `73 passed`。註：未做 live create 以免在 repo 寫入測試檔；邏輯以單元＋API 測試覆蓋。
- [x] `layer-style-preset-catalog`（openspec change，2026-06-20）：style preset 改「索引 + 單檔 detail」分層,避免 list 全掃。儲存:`style_presets/agent/index.json`（輕量:id/name/profiles/資源摘要）+ `style_presets/agent/presets/<id>.json`（完整食譜），移除單一 `catalog.json`。core 抽出共用純函式 `build_summary`/`validate_preset_against`/`compose_preset`，`FileStylePresetProvider`（in-memory，測試用）保留並加 `list_summaries`/`reindex`；新增 `DirStylePresetProvider`（`list_summaries` 只讀 index、`get_preset`/`compose` 讀單檔、`validate_presets` 報 index↔detail 漂移、`reindex` 重建、index 不存在時自我修復）；`get_default_provider`→`DEFAULT_AGENT_DIR`。API list 改用 `list_summaries`、新增 `POST /api/style-presets/reindex`；MCP 新增 `reindex_style_presets`（tool 數 30→31）。測試:backend 新增 `test_style_preset_layering.py`（8）、mcp-server +1；全量 backend `218 passed`、mcp-server `71 passed`；既有 28 style-preset 測試不變。
- [x] style-preset 目錄重組（2026-06-20）：把機器與人類文件合併到專案根目錄單一 `style_presets/`，以 `agent/`（機器可讀，`agent/catalog.json`）與 `human/`（人類筆記，`_template.md`/`<preset-id>.md`）分開（取代原本分散的 `backend/style_presets/` + `docs/style-presets/`）。新增 `style_presets/README.md` 說明分工。改 `DEFAULT_CATALOG_PATH` → `style_presets/agent/catalog.json`（`from_file` 的 `parent.parent.parent` 推 project_root 不變，note_path 仍以 repo-root 相對解析）；catalog 的 `note_path` 與 human note 的 `catalog_path` frontmatter 同步更新。MCP tool 與 API 路由（`/api/style-presets`）為網路路徑、不受影響。驗證：新路徑載入 creator-a、note_path/frontmatter 驗證通過、backend `210 passed`、mcp-server `70 passed`。
- [x] `persist-full-workflow-for-rerun`（openspec change，2026-06-20）：在 `GeneratedImage` 新增 `workflow_json`/`source_image`/`source_mask` 三個 nullable 欄位（`models.py`），並加 `database.init_db()`（create_all + 對既有表 ALTER 補欄，idempotent；專案無 migration 框架）於 `main.py` lifespan 啟動時跑——已對既有 stale `auto_draw.db` 驗證補欄成功。`recording.save` 接收並儲存三欄（workflow dict 自動序列化為 JSON）。`queue._process_pending` 在送出前把「實際送出的完整 prompt」存入 `job.params['workflow_json']`、保留來源圖/遮罩 gallery 相對路徑；`_check_running_complete` 傳入 recording。`gallery.rerun_image` 分支：有 `workflow_json` → 走 `submit_custom` 忠實重現（重上傳 source_image/source_mask、沿用 baked seed 不重隨機、來源檔不存在回 409），無則退回原欄位重建走 template 路徑。**解鎖 add-agent-workflow-authoring #5**（回填可由 DB 的 workflow_json 取形狀，閘門查 DB 成功記錄，避開記憶體 job 狀態的跨進程不同步）。測試：backend 新增 `test_persist_workflow.py`（5：recording 持久化、txt2img 留空、rerun custom/legacy/缺來源）；全量 backend `194 passed`（4 既有失敗無關）。註：本機嘗試 live 全流程生圖時遇 worker「completed but no output images」競態（既有問題、與本次無關），故 workflow_json 的 live 持久化未在本機 run 觀察到，邏輯以單元測試覆蓋。
- [x] `enhance-custom-workflow-tool`（openspec change，2026-06-18）：`generate_image_custom_workflow` 補上 `image`（img2img 主體）、`mask`（inpaint）、`batch_size`、`diffusion_model`/`text_encoder`/`vae` 參數通道。**Breaking（僅 custom 路徑）**：`apply_params` 改為「僅在呼叫端明確提供時才覆寫」`steps`/`cfg`/`seed`，省略時保留提交 workflow JSON 原值（解決 hires-fix / 多 KSampler 被強制覆寫的問題）；預設值（20/7.0）與隨機 seed 的職責從 `apply_params` 移到 `queue.py` 的 template 分支，`generate_image`（template 路徑）行為不變。新增 `LoadImageMask` 注入（獨立 class_type，與 subject/pose 的位置式注入無衝突）與 `backend/workflows/inpaint.json` 模板。動到 `backend/app/core/{workflow,queue}.py`、`backend/app/schemas/generate.py`、`backend/app/api/generate.py`、`mcp-server/mcp_server/tools/generate.py`。backend `121 passed`（4 個既有失敗與本次無關：`test_relative_defaults_are_project_root_based_from_any_cwd`、兩個 `test_lora_trainer` mock 案例、`honoka_pose_controlnet` 缺檔模板）、mcp-server `45 passed`。

### 路徑正規化修正（2026-06-09）
- [x] `backend/app/config.py` 將 DB / output / gallery / lora_train / sd-scripts / watch_dirs 的相對路徑統一正規化為 **project root 基準**
- [x] 修正 `DATABASE_URL` 為 sqlite 相對路徑時會隨啟動 cwd 漂移的問題
- [x] 新增 `backend/tests/test_config_paths.py`，驗證 defaults 與 env override 在不同 cwd 下都解析到同一位置
- [x] 實測 `POST /api/generate/` 成功，輸出與 DB 記錄固定落在 `backend/` 路徑下

### F-F：LLM 自動產生 Caption (2026-06-07)
- [x] 後端 LLM caption API：`POST /api/lora-docs/caption-llm/{image_path}`
- [x] MCP tool：`caption_image(image_path)`
- [x] 新增 `llm_caption_url` 環境變數（`.env`）
- [x] 支援外部 LLM API（如 BLIP2 service）
- [x] 完成驗證：呼叫 MCP tool → LLM 產生 → 寫入 .txt

### 系統基礎建置
- [x] ComfyUI API 串接（`backend/app/core/comfyui.py`）
- [x] Workflow JSON 模板管理（`backend/app/core/workflow.py`）
- [x] 批次生圖排程器（`backend/app/core/queue.py`）
- [x] 資料庫設計（`backend/app/db/models.py`）
- [x] 自動記錄 Pipeline（`backend/app/core/recording.py`）
- [x] Gallery 瀏覽器 + 一鍵重現（`backend/app/api/gallery.py`）
- [x] LoRA 文件工具（watcher、caption editor、zip download）
- [x] LoRA 訓練執行器（`backend/app/services/lora_trainer.py`）
- [x] MCP Server 基礎建置（`mcp-server/`）
- [x] MCP Tools 初版（generate、lora_train、gallery）
- [x] Docker 部署
- [x] F-B Job 狀態查詢（DB job_id + API endpoint + MCP tool）
- [x] F-C Job 取消（`queue.cancel()` + `DELETE /api/generate/queue/{job_id}` + MCP tool，PR #4，2026-06-07）
- [x] F-D 查詢可用資源（純 MCP tool，API 已有）
- [x] F-A 生圖完整參數（lora_strength、denoise、width/height、sampler_name、scheduler 暴露至 MCP，2026-06-07）
- [x] F-E LoRA 訓練完整參數（移除 generate_after + MCP 補齊參數，PR #5，2026-06-07）

### Phase 0：Slack 清理（PR #1，2026-06-04）
- [x] 刪除 Slack service 檔案
- [x] 移除 main.py Slack 啟動邏輯
- [x] 移除 config Slack token 欄位
- [x] 移除 schema slack_channel_id / slack_thread_ts
- [x] 更新 README / AGENTS 移除 Slack 相關章節

---

## 進行中

- [x] `add-video-generation-mcp`（openspec change，2026-07-06 完成）：backend / MCP video MVP 已完成並通過 live runtime smoke。具備 generic `GeneratedArtifact` lifecycle、artifact output detection（png/jpg/jpeg/webp/gif/mp4/webm）、queue 完成後複製 artifact 至 gallery、成功無可記錄 artifact 時 `recording_error` failed、`GET /api/gallery/artifacts/{id}`、generation status `artifacts[]`（保留 legacy `image_id` / `image_path`）、MCP `get_gallery_artifact`、MCP/API `generate_video_custom_workflow`、video input refs（`image` / `first_frame` / `last_frame` / `video_ref`）、video template tags（`txt2video` / `img2video` / `first_frame` / `last_frame` / `video_ref` / `audio_ref`）、video job artifact gate for `save_workflow_template`，以及 MCP docs 的 derivation loop / MVP out-of-scope guardrails。Batch gate 複查：`backend/workflows/gen_img2video_wan_first_frame*.meta.json` 與 `gen_img2video_wan_5keyframe_single_workflow.meta.json` 均為 valid manifest 且 workflow 存在。Live smoke：用 MCP `generate_video_custom_workflow` 提交 `gen_img2video_wan_first_frame` 低負載 job `ea5f0106-421e-4e32-b113-47218bd888e7`（256x256、length=9、steps=1、cfg=1.0），完成後 `artifacts[]` 含 video artifact `1186`；`get_gallery_artifact(1186)` 回 mp4，local file `/Volumes/AI-Drawing-16T/ai-drawing/gallery/2026-07-06/openspec_gate_lowload_wan_smoke_00001_ea5f0106_0.mp4` 存在且 size=45970；已 `free_comfyui_memory` 成功。
- [ ] `add-agent-workflow-authoring`（openspec change，2026-06-19 起）：把重心從「人供給固定模板」轉成「供給 agent 能力＋護欄」，讓 agent 自組 workflow 為主路徑、模板庫自我擴充。6 個階段，**一次做一階段、階段間 review**。
  - [x] #1 ComfyUI 節點 schema grounding（單點查）：`ComfyUIClient.get_object_info`（以 base_url 為 key 的程序內 TTL 快取，新增 `comfyui_object_info_ttl` 設定，可 `force_refresh`／`clear_object_info_cache` 失效）＋純函式 `search_node_types`（名稱＋類別雙條件 AND，回 `{name, category}`）／`list_node_categories`／`extract_node_schema`；新增 `GET /api/comfyui/nodes`（依 query/category 搜尋）、`/api/comfyui/node-categories`（列類別＋數量）、`/api/comfyui/nodes/{node_type}`（單節點 input/output 規格，缺則 404），於 `main.py` 註冊；MCP 新增 `search_nodes`（query＋category）／`list_node_categories`／`get_node_schema` 三個 agent-friendly tool（404→`not_found`）。測試：backend `test_comfyui_nodes.py`（15）、mcp-server `test_comfyui_tools.py`（6）。**真機驗證**：對 live ComfyUI（8188, 747 節點）實測名稱搜尋、類別搜尋（`category=loaders` 找到 CLIPLoader/DualCLIPLoader/UNETLoader/VAELoader 等 text-encoder 載入器）、query+category 組合、單節點 schema、未知節點 404/not_found、TTL 快取命中（0ms 同物件）vs `force_refresh` 重抓；MCP→backend→ComfyUI 全鏈路通。backend 在本機新建 `backend/.venv`（py3.11，本機原僅 3.9 系統＋3.10 bare），4 個既有失敗與本次無關（config-path cwd、兩個 lora_trainer mock、workflow bbox detector）。
  - **Context 防護**（2026-06-19）：雙重把關——(1) `search_nodes` 的 `query`/`category` **至少要給一個，皆空回 400**（backend 端，連 `/object_info` 都不抓；MCP 端同步先擋回 `missing_filter` 不打 backend），擋「完全不篩」；(2) 每頁**上限**（預設 50、上限 200），擋「篩了但範圍太寬」；(3) 加 `offset` **分頁**（命中按名稱排序、回 `offset`/`next_offset`/`truncated`），讓超過上限的尾巴仍可翻頁取得（避免「永遠拿不到後面」），但 `next` 仍引導優先縮小條件、翻頁為退路。真機驗證 `category=model` 274 個 → 6 頁全取回。`list_node_categories` 維持**無參數**作為探索入口（先瀏覽 144 類再 `search_nodes(category=…)`）。`get_object_info` 原始 1.2MB 僅 backend 內部用、永不回給 agent。COMBO 維持壓成 `"COMBO"` 不回可選值（定案，可選值走 `list_available_resources`）。真機驗證：無參數→400、`category=loaders`→35 筆、`node-categories`→144 類。spec 新增「依名稱/類別搜尋」與「搜尋有界以保護 context（強制給參數＋上限）」requirement，`openspec validate` 通過。
  - [x] #2 模板能力 manifest＋索引（受控詞彙二元標籤）：儲存採 **sidecar**（`backend/workflows/<name>.meta.json`，與圖同生共死、索引只 glob 小檔不讀 workflow JSON）。新增 `app/core/workflow_manifest.py`（受控詞彙 `CONTROLLED_VOCABULARY`：modality 單值 txt2img/img2img/inpaint、conditioning 集合、io 集合 text/image_ref/mask、model_family 單值選用；`parse_manifest`/`validate_manifest`〔詞彙外標籤、缺 modality、id 不符檔名、workflow 缺檔皆報問題〕/`load_manifests`）。為 7 個現有模板撰寫 manifest 並全數通過驗證（modality 依 latent 來源判定：controlnet_pose/img2img_lora_pose 走 VAEEncode→img2img，txt2img_lora_pose 走 EmptyLatentImage→txt2img；anima 標 model_family=anima；lora 不入詞彙，屬 runtime 注入）。新增 `app/api/workflow_catalog.py`（`GET /api/workflow-catalog/` 輕量索引、`/validate` 逐模板驗證），於 main.py 註冊；MCP 新增 `list_template_capabilities`／`validate_template_capabilities`（tool 數 25→27）。測試：backend `test_workflow_manifest.py`（8）、mcp-server `test_workflow_catalog_tools.py`（2）；全量 backend `175 passed`（4 既有失敗無關）。真機驗證索引/validate 端點。
  - [x] #3 二元 reuse 匹配（superset 測試）：`workflow_manifest.py` 新增 `CapabilityRequest` + `manifest_covers`（逐欄位 AND：modality 必相等、model_family 需求給才比、conditioning/io 需求須為模板子集，純集合無分數）+ `find_matching_templates`（只匹配 valid manifest，壞模板不可 reuse，依 id 排序）。新增 `GET /api/workflow-catalog/match`（modality 必填、皆空回 400，conditioning/io 逗號分隔）與 MCP `match_workflow_template`（命中→提示 generate_image(template=…)、miss→提示自組）。tool 數 27→28。測試：backend `test_workflow_match.py`（8，含模板涵蓋命中、缺能力 miss、modality 不符即 miss、model_family 僅在指定時約束、invalid 不可匹配）、mcp-server +2。真機驗證：img2img+pose→[controlnet_pose,img2img_lora_pose]、inpaint+mask→[inpaint]、txt2img+anima→[anima]、txt2img+sd15→[]（strict miss）、缺 modality→400。全量 backend `184 passed`（4 既有失敗無關）。
  - [x] #4 custom workflow 轉發 ComfyUI 驗證錯誤：新增 `comfyui.structure_node_errors`（把 ComfyUI node_errors 整理成 `[{node_id,class_type,reason}]`，class_type 可由 workflow 回填）。queue worker 對 `ComfyUIError` 改為**永久失敗、不重試**（修掉 `_pending.insert(0,job)` 的隊首阻塞 bug——壞 workflow 不再無限重試堵住佇列），新增 `_failed` 環狀儲存（上限 100）；`FileNotFoundError`/連線錯誤亦記為 failed（不再靜默消失）。`get_job_status` 回 failed 狀態；`GET /api/generate/job/{id}` 對 failed 透傳 `error`＋`node_errors`；MCP `get_generation_status` 新增 failed 分支（ok=false＋node_errors＋引導修正重送），`generate_image_custom_workflow` 改回 agent-friendly JSON。測試：backend `test_queue_failure.py`（5，含結構化、不重試、API 透傳）、mcp-server +1；全量 backend `189 passed`、mcp-server `67 passed`。**真機驗證**：壞 sampler_name → `node_errors=[{node_id:3,class_type:KSampler,reason:"Value not in list..."}]`，佇列保持乾淨。
  - [x] #5 回填／自我擴充模板庫（成功閘門＋key 去重＋家族歸檔＋版本化不就地改）：新增 MCP `save_workflow_template(job_id, modality, model_family, conditioning, io, description)` + `POST /api/workflow-catalog/backfill`。**閘門查 DB**：以 job_id 查 `GeneratedImage` 成功記錄且需有 `workflow_json`（無→404/409，跨進程/重啟皆成立，靠 persist-full-workflow + harden-queue 兩前置）。`workflow_manifest.py` 新增 `deprecated` 欄（不被 reuse）、`capability_key`、`strip_workflow_to_shape`（重用 `apply_params(seed=0,prompt="",negative_prompt="")` 剝一次性內容）、`backfill_template`（詞彙驗證→能力 key 去重〔有效同 key→reused〕→無同 key 建新檔、id 編入家族 `gen_<modality>_<family>[_cond][_io]`→剝形狀寫 sidecar；壞的同 key→出新版本並把舊的標 deprecated，永不就地改 graph、不合併圖）。`find_matching_templates` 排除 deprecated。tool 數 28→29。測試：backend `test_template_backfill.py`（9）；全量 backend `209 passed`、mcp-server `69 passed`。註：live 端到端回填需一個成功完成的 job（本機受 ComfyUI 積壓未取得），邏輯以單元測試含 DB 閘門覆蓋。
  - [x] #6 agent 指引＋consolidation：串起「match→build→backfill」迴圈的指引——更新 `generate_image_custom_workflow` 成功 `next` 指向 `save_workflow_template`；改寫 `mcp-server/README.md`（24→30 tool、移除已停用的 *_from_description、新增「ComfyUI 節點 Schema」與「Workflow 模板能力目錄」兩節 + 建議自組流程、標註 get_generation_status 失敗回 node_errors）。新增 consolidation 清理 deprecated 模板：`consolidate_templates` core + `POST /api/workflow-catalog/consolidate` + MCP `consolidate_workflow_templates`（手動／週期，tool 數 29→30）。測試：backend +1（consolidate 只刪 deprecated）、mcp-server +1；全量 backend `210 passed`、mcp-server `70 passed`。真機 smoke：能力索引（含 deprecated 欄）、backfill 閘門未知 job→404、consolidate 端點皆正常（generate→backfill 全程因 ComfyUI 積壓未在本機觀察，邏輯以單元測試＋DB 閘門覆蓋）。**add-agent-workflow-authoring 全 6 階段完成。**
  - artifacts：`openspec/changes/add-agent-workflow-authoring/`（proposal/design/specs/tasks，已 `openspec validate` 通過）。

---

## 待做

OpenClaw × ai-drawing 本地繪圖 / MCP 整合：

1. [x] 透過 ai-drawing backend 端點進行低負載繪圖驗證
2. [x] 整理 OpenClaw backend 繪圖 SOP
3. [x] 將 ai-drawing 繪圖最小閉環做成 MCP tools
4. [x] 透過 MCP 實際完成一次繪圖驗證
5. [x] 整理 OpenClaw MCP 繪圖 SOP

詳細順序、狀態與驗證標準見 `docs/openclaw-ai-drawing-mcp-handoff.md`。

---

## 卡住 / 待決策

| 項目 | 原因 | 需要 |
|------|------|------|
| `fix-mcp-tool-catalog-exposure` live MCP/backend network smoke（2026-07-06） | 目前 sandbox 禁止 bind localhost：啟動 `uvicorn app.main:app --host 127.0.0.1 --port 8001` 回 `[Errno 1] ... operation not permitted`；既有 backend 也未在 `8001` listening。已用 server-side MCP registration smoke + FastAPI in-process resource smoke 補驗，未提交任何 generation/training job。 | Hermes 或本機非 sandbox 環境重啟 MCP client/backend 後，重跑真正 stdio/client-visible tool catalog 與 `list_available_resources` live smoke。 |
| backend 生圖 queue 隊首阻塞（2026-06-16） | `backend/app/core/queue.py` 在 submit failure 時會把 job 插回隊首，可能造成壞 job 無限重試並堵住後續任務 | 修 queue failure-handling（retry_count / last_error / retry 上限 / failed 狀態）；詳見 `docs/backend-generate-queue-head-blocking-2026-06-16.md` |
| Skill 文件 | openclaw skill 格式未確認 | 確認工具名稱 / 框架後才能開始 |
